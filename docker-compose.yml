version: '3.8'

services:
  api:
    build:
      context: .
      dockerfile: ApiPrincipal/Dockerfile
    env_file:
      - ApiPrincipal/.env
    ports:
      - "5000:80"
    container_name: api-principal
    restart: always
